# Assets

Use this folder for optional character reference images, selected generated clips, thumbnails, and audio notes.

Large generated media folders such as `generated` and `exports` are ignored by Git by default.
