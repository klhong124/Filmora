# Assets

Use this folder for prompt references, thumbnail concepts, selected image outputs, audio notes, and production assets.

Large generated media folders such as `generated` and `exports` are ignored by Git by default.
